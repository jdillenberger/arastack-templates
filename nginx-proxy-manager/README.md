# Nginx Proxy Manager

Nginx Proxy Manager provides a web UI for managing Nginx reverse proxy configurations with built-in Let's Encrypt SSL certificate provisioning. It simplifies exposing web services with HTTPS without needing to edit Nginx config files manually.

**Website:** [nginxproxymanager.com](https://nginxproxymanager.com)
